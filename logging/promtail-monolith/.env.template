# The central Loki instance from ../ (logging/docker-compose.yml), reached
# over the LAN -- this is almost always a different host than the one
# running backend/serve.py.
LOKI_URL=http://192.168.4.100:3100
